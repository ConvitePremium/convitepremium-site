const W='558894897535';let convites=[],cat='todos';const grid=document.getElementById('catalogGrid'),input=document.getElementById('searchInput'),empty=document.getElementById('emptyState'),filters=document.getElementById('filterList'),modal=document.getElementById('videoModal'),vid=document.getElementById('modalVideo');function norm(t){return String(t||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}function wa(m){return `https://wa.me/${W}?text=${encodeURIComponent(m)}`}async function init(){convites=await fetch('data/convites.json').then(r=>r.json());[...new Set(convites.map(c=>c.categoria))].forEach(c=>{filters.insertAdjacentHTML('beforeend',`<button class="filter" data-category="${c}">${c}</button>`)});render()}function card(c){return `<article class="model-card"><div class="thumb" data-open="${c.id}"><span class="tag">${c.selo}</span><img src="${c.thumbnail}" alt="${c.nome}" loading="lazy"><div class="play"><span>▶</span></div></div><div class="info"><p class="cat">${c.categoria}</p><h3>${c.nome}</h3><p>${c.descricao}</p><div class="card-actions"><button class="btn outline" data-open="${c.id}">Assistir modelo</button><a class="btn gold" target="_blank" href="${wa('Olá! Gostei do modelo '+c.nome+' no site Convite Premium.')}">Solicitar</a></div></div></article>`}function render(){let q=norm(input.value);let list=convites.filter(c=>(cat==='todos'||c.categoria===cat)&&norm(c.nome+' '+c.categoria+' '+c.descricao+' '+c.selo).includes(q));grid.innerHTML=list.map(card).join('');empty.style.display=list.length?'none':'block'}filters.onclick=e=>{let b=e.target.closest('.filter');if(!b)return;document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');cat=b.dataset.category;render()};input.oninput=render;document.onclick=e=>{let o=e.target.closest('[data-open]'),cl=e.target.closest('[data-close]');if(o){let c=convites.find(x=>x.id===o.dataset.open);document.getElementById('modalCategory').textContent=c.categoria;document.getElementById('modalTitle').textContent=c.nome;document.getElementById('modalDescription').textContent=c.descricao;document.getElementById('modalWhatsapp').href=wa('Olá! Gostei do modelo '+c.nome+' no site Convite Premium.');vid.innerHTML=`<source src="${c.video}" type="video/mp4">`;vid.load();modal.classList.add('active');document.body.style.overflow='hidden'}if(cl){modal.classList.remove('active');vid.pause();vid.innerHTML='';document.body.style.overflow=''}};document.onkeydown=e=>{if(e.key==='Escape'){modal.classList.remove('active');vid.pause();vid.innerHTML='';document.body.style.overflow=''}};init();